import Loader from 'react-loader-spinner';

export default function Loaders() {
  return (
    <div>
      <Loader
        type="ThreeDots"
        color="#18166b"
        height={80}
        width={80}
      />
    </div>
  );
}
